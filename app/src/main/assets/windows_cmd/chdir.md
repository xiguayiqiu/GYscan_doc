# chdir 命令

## 命令说明
切换目录

## 语法
```
chdir [目录路径]
```

## 功能
更改当前工作目录，与 `cd` 命令功能相同。

## 示例
```
chdir C:\Users
chdir ..
chdir 
```

## 注意事项
- 不带参数时，显示当前目录的完整路径
- 使用 `..` 表示上一级目录
- 使用 `\` 表示根目录
- 可以使用相对路径或绝对路径
- 是 `cd` 命令的别名
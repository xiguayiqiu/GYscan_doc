# set 命令

## 命令说明
设置环境变量

## 语法
```
set [变量名]=[值]
```

## 功能
设置或显示环境变量。

## 示例
```
set PATH=C:\Windows\System32
set JAVA_HOME=C:\Java\jdk1.8.0
set
```

## 注意事项
- 用于设置或显示环境变量
- 不带参数时显示所有环境变量
- 变量值可以包含空格，需要用引号括起来
- 可以在批处理文件中使用，设置临时变量
# tree 命令

## 命令说明
显示目录树

## 语法
```
tree [目录路径] [选项]
```

## 功能
以树状结构显示指定目录及其子目录。

## 选项
- `/f`：显示目录中的文件
- `/a`：使用 ASCII 字符而不是图形字符

## 示例
```
tree
tree C:\Windows /f
tree /a
```

## 注意事项
- 不带参数时，显示当前目录的树状结构
- 可以使用相对路径或绝对路径
- 使用 `/f` 选项会显示每个目录中的文件
- 使用 `/a` 选项会使用 ASCII 字符绘制树状结构，适用于纯文本环境
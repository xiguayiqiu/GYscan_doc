# flattemp 命令

## 命令说明
临时文件夹管理

## 语法
```
flattemp [选项]
```

## 功能
管理临时文件夹设置。

## 选项
- `/enable`：启用扁平化临时文件夹
- `/disable`：禁用扁平化临时文件夹
- `/query`：查询当前设置

## 示例
```
flattemp /enable
flattemp /disable
flattemp /query
```

## 注意事项
- 用于管理临时文件夹的扁平化设置
- 扁平化临时文件夹可以减少临时文件的嵌套层级
- 需要管理员权限才能使用
# pushd 命令

## 命令说明
推送目录

## 语法
```
pushd [目录路径]
```

## 功能
将当前目录添加到目录栈并切换到指定目录。

## 示例
```
pushd C:\Windows
popd
```

## 注意事项
- 与 `popd` 命令配合使用
- 用于在目录之间快速切换
- 目录栈遵循后进先出（LIFO）原则
- 可以使用网络路径，会自动映射网络驱动器
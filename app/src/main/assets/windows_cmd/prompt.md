# prompt 命令

## 命令说明
设置命令提示符

## 语法
```
prompt [提示字符串]
```

## 功能
设置命令提示符的格式。

## 示例
```
prompt $p$g
prompt $t $p$g
prompt Hello>$g
```

## 注意事项
- 用于自定义命令提示符的显示格式
- 可以使用特殊字符来显示当前目录、时间等信息
- 常见的特殊字符：
  - `$p`：显示当前目录
  - `$g`：显示 > 符号
  - `$t`：显示当前时间
  - `$d`：显示当前日期
  - `$v`：显示Windows版本
- 不带参数时，恢复默认提示符
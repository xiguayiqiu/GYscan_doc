# ren 命令

## 命令说明
重命名文件

## 语法
```
ren [旧文件名] [新文件名]
```

## 功能
重命名文件或目录。

## 示例
```
ren oldfile.txt newfile.txt
ren *.txt *.bak
```

## 注意事项
- 可以使用通配符（如 `*.txt`）来重命名多个文件
- 可以使用相对路径或绝对路径
- 是 `rename` 命令的别名
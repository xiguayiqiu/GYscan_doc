# whoami - 显示当前用户名

## 1. 命令简介
whoami 命令用于显示当前登录用户的用户名，是 Linux 系统中最常用的命令之一。

## 2. 命令语法
```bash
whoami
```

## 3. 实战示例

### 3.1 显示当前用户名
```bash
whoami
```

## 4. 退出状态
- 0：命令执行成功
- 非0：命令执行失败

## 5. 注意事项
- whoami 命令不需要任何参数就可以执行
- whoami 命令的输出可以用于脚本中，以获取当前登录用户的用户名
- 与 `id -un` 命令的功能相同
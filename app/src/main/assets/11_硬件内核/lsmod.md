# lsmod - 列出加载的模块

## 1. 命令简介
lsmod 命令用于列出加载的内核模块，是 Linux 系统中常用的硬件内核命令之一。

## 2. 命令语法
```bash
lsmod
```

## 3. 实战示例

### 3.1 列出加载的模块
```bash
lsmod
```

## 4. 退出状态
- 0：命令执行成功
- 非0：命令执行失败

## 5. 注意事项
- lsmod 命令用于列出加载的内核模块
- lsmod 命令实际上是读取 /proc/modules 文件并格式化输出
- lsmod 命令是一个常用的系统管理工具，用于查看内核模块的加载状态
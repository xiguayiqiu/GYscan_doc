# logout - 退出登录

## 1. 命令简介
logout 命令用于退出当前登录会话，是 Linux 系统中常用的命令之一。

## 2. 命令语法
```bash
logout
```

## 3. 实战示例

### 3.1 退出登录
```bash
logout
```

## 4. 退出状态
- 0：命令执行成功
- 非0：命令执行失败

## 5. 注意事项
- logout 命令不需要任何参数就可以执行
- logout 命令只能在登录Shell中使用，不能在非登录Shell中使用
- 与 exit 命令不同，logout 命令专门用于退出登录会话
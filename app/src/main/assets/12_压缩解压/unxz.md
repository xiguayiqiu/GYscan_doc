# unxz - 解压缩文件

## 1. 命令简介
unxz 命令用于解压缩文件，是 Linux 系统中常用的压缩解压命令之一。

## 2. 命令语法
```bash
unxz [选项] [文件]
```

## 3. 常用选项
| 选项 | 说明 |
|------|------|
| -f | 强制解压缩 |
| -k | 保留原文件 |
| -l | 显示压缩信息 |
| -r | 递归解压缩 |
| -t | 测试压缩文件 |
| --help | 显示帮助信息 |
| --version | 显示版本信息 |

## 4. 实战示例

### 4.1 解压缩文件
```bash
unxz file.txt.xz
```

### 4.2 强制解压缩
```bash
unxz -f file.txt.xz
```

### 4.3 保留原文件
```bash
unxz -k file.txt.xz
```

### 4.4 显示压缩信息
```bash
unxz -l file.txt.xz
```

## 5. 注意事项
- unxz 命令用于解压缩文件，处理 .xz 后缀的压缩文件
- unxz 命令默认会删除原压缩文件，使用 -k 选项可以保留原文件
- unxz 命令是一个常用的压缩工具，用于文件解压缩